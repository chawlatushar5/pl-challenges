# ShopFlow (lite)

A small e-commerce backend. Products belong to categories; orders reference products through order items.

Schema lives in `prisma/schema.prisma`. API route stubs are under `src/routes/`, shared types under `src/types/`, and UI form components under `src/components/`.
