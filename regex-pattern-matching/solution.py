def is_match(s: str, p: str) -> bool:
     """
     Given input string s and pattern p, implement regex matching that
     supports '.' and '*' with the following semantics:
       '.' matches any single character.
       '*' matches zero or more of the preceding element.
     The match must cover the ENTIRE input string (not partial).
     '*' is always preceded by a valid character or '.', per problem
     constraints — you don't need to validate malformed patterns.
     """
     # TODO: implement
     raise NotImplementedError