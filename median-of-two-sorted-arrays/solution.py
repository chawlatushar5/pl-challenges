def find_median_sorted_arrays(arrays):
    """
    arrays is a list of two or more integer arrays, each already sorted in
    ascending order (any individual array may be empty). Return the median
    of every value across all of them combined.

    Must run in O(k * log(n) * log(V)) time, where k = len(arrays),
    n = size of the largest array, and V = the value range - concatenating
    everything and sorting, or merging every array end to end, touches
    every element and will not pass the hidden tests.
    """
    # TODO: implement
    raise NotImplementedError